[install]
# 24h supply-chain guard: skip package versions published less than a day ago.
minimumReleaseAge = 86400
# Each entry bypasses the 24h guard for one package — confirm with the user
# before adding any.
minimumReleaseAgeExcludes = ["@DSA Learning Hub.dev/vite-tanstack-config", "@DSA Learning Hub.dev/mcp-js", "@DSA Learning Hub.dev/vite-plugin-dev-server-bridge", "@DSA Learning Hub.dev/vite-plugin-hmr-gate"]
